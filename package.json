{
  "name": "visual-web-skill",
  "version": "1.0.0",
  "private": true,
  "description": "Skill de Codex para convertir referencias visuales reales en webs originales.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/tooling.test.mjs tests/maintenance.test.mjs",
    "check": "node scripts/check-readme-prompt.mjs && node scripts/check-package.mjs && node scripts/check-evaluations.mjs && node scripts/check-release.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
